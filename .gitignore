# --- Android / Gradle ---
.gradle/
.kotlin/
build/
**/build/
local.properties
captures/
*.apk
*.aab
*.hprof

# Signing keys and service configs never belong in the repo
*.jks
*.keystore
keystore.properties
google-services.json

# Thmanyah font files: licensed for embedding in the built app only, and the
# license forbids uploading/hosting them. Never commit (this repo is public).
# Download from https://font.thmanyah.com/ and place the OTFs in this folder;
# without them the app falls back to Amiri for Arabic. See README.
app/src/main/res-licensed/

# --- IDEs / OS ---
.idea/
*.iml
*.ipr
*.iws
.vscode/
.DS_Store

# --- Archived Flutter app (archive/flutter-app) ---
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
*.class
*.log
*.pyc
*.swp
